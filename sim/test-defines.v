// Required include for the Micron mt48lc16m16a2 behavioral model.
// Intentionally empty — this project doesn't use any of the optional
// defines the model checks for (INIT_CLEAR_MEM_BANKS, PRELOAD_RAM);
// density is selected via +define+MT48LC{32,16,4}M16 on the vlog/vsim
// command line instead (see mt48lc16m16a2.v and README.md).
